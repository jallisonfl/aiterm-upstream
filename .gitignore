# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.remember/

# Claude Code worktrees
.claude/worktrees/

# Internal notes, not for the public repo
PORTING-TO-OPCODE.md

# Agent tooling config — local only, never published
.claude/

# Files that hand rules to an agent or to contributors. Kept out of the repo on
# purpose: one of these landing in a clone quietly governs every agent that opens
# it. Ignored rather than merely deleted so `git add .` cannot bring one back.
AGENTS.md
CLAUDE.md
GEMINI.md
BRANCHING.md
CONVENTIONS.md
.cursorrules
.windsurfrules
.github/copilot-instructions.md
.cursor/
.aider*

# Relay: server build output + machine-local secrets, protocol crate build output.
relay/server/target/
relay/server/relay.json
relay/protocol/target/
relay/protocol/Cargo.lock

# Phone app build output. The Gradle wrapper is committed on purpose so a clone
# builds without a local Gradle install.
mobile/build/
mobile/app/build/
mobile/.gradle/
mobile/local.properties

# Local harness audit notes — never published.
harness-audit/
